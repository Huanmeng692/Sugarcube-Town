昨天，我们已经成功的完成了，使用VSCode创作Sugarcube游戏……的第一步。
成功的在完全不使用Twine的情况下，构建出了第一个可以打开，并且还能正常运行的游戏（虽然只有一页）
（并且UI还十分的难看）

今天，我们的目标是，让我们的游戏看上去更像是一个游戏。
毕竟，游戏一定要能玩！！！
至少，能够点击！

这个时候，我们就要想了，一个HTML游戏，或者说，一个游戏，打开之后呈现在玩家面前的第一幅画面应该是什么样子的？
一上来就是紧张刺激的捏人环节吗？
那样的话未免太难看了，不仅玩家不喜欢，你花费的精力心血也会因此而白费。
即便是网络小说，也是有黄金三章的说法的，游戏这种交互性很强的媒介，当然更要注重一开始的体验了。

所以，我们今天的第一个目标，是制作一个开场的画面。

这算是哪门子的开场画面啊，画面在哪里呢？
至少画面要能把游戏UI遮挡住吧？

不要着急，遮挡马上就来。
刚刚我们在片段中增加了一个ID为opening的div，我们现在要做的就是让这个div遮盖住整个浏览器窗口。
包括将Sugarcube的UI界面也遮盖住，这个要怎么做呢？
当然是CSS了。
我们新建一个css文件，文件名随意，重点是内容
对了，CSS文件的后缀名是css，这个应该不用多说吧？
我们可以用div的ID来选中这个元素，然后给它增加样式。

好难看啊，字体太小，而且没有水平居中，这算是什么开场画面啊……
不要着急，我们一步步来，先解决水平居中和字体大小的问题。

我们刚刚增加了一个div，套在了原本的div里面，用来实现文字的效果。
为什么要增加这个div呢，主要是为了实现文字高度的设置。

现在我们再试一试

虽然依旧不好看，但至少像是那么一回事了。

但是

这个时候，我们发现没办法开始游戏，因为这个开场画面，把我们的游戏的主界面完全遮挡住了。
既然我们已经写了，点击任意处开始游戏。
那我们就一定要实现才可以，不然会被笑话的。
而这个功能并不难，用Javascript可以很轻松的做到这一点。
具体是怎么做呢？
首先我们需要给开场画面增加一个点击后，将这个画面移除的效果。
这有两个难点，一个是如何获得这个div元素，以及如何操作它。
我们一步一步的来讲。

获得HTML元素，需要用到JS自带的DOM方法，而要做到这一点，我们首先需要这个元素存在于页面上
也就是等待浏览器将页面加载完成
而Sugarcube提供了一个很方便的功能来确保这一点。那就是<<done>>宏，字面意义，这个宏的作用是
将内部包裹的代码延迟到页面完全加载完毕之后执行。
而执行的内容，是Javascript脚本，我们又需要用到另一个宏，也就是<<script>>
这个宏内部包裹的代码，浏览器会视作纯Javascript来执行，所以我们第一步要写的内容是：

紧接着是里面的代码部分，为了方便编写代码，我们可以先创建一个JS文件，以便于使用VSCode的代码补全功能。
不要忘记删除js文件，因为它已经没用了
现在我们试一下效果如何。
是不是很酷？